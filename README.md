# Usage

```r
fit <- get_model(fixed_effects,
                 random_effects,
                 effect_size,
                 effect_sd,
                 response,
                 family)
get_power <- function(fit = fit,
                      fixed_effects = NULL,
                      random_effects = NULL,
                      effect_size = NULL,
                      effect_sd = NULL,
                      response = NULL,
                      family = NULL)
```